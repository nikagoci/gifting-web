const Reducer = (state, action) => {
    switch(action.type) {
        case "BLA":
            return {
                
            };

        default: 
            return state
    }
}

export default Reducer